<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sv.proye.tecaapp" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_INTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/book_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.TecaApp" >
        <activity
            android:name=".views.ui.usuarios.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".views.ui.usuarios.RegistrarseActivity"
            android:exported="false" />
        <activity
            android:name=".views.MenuInicioActivity"
            android:exported="false"
            android:label="@string/title_activity_menu_inicio"
            android:theme="@style/Theme.TecaApp.NoActionBar" />
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>